# Database (SQLite by default — zero setup, file lives in /prisma)
DATABASE_URL="file:./dev.db"

# NextAuth — REQUIRED. Generate a random string with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# and paste the output below (do NOT leave this blank).
AUTH_SECRET="T4gV96V96KL9oGg9Fdtcz48KJiVocT8T+up6xxCB9Fk="
NEXTAUTH_URL="http://localhost:3000"

# Google OAuth — https://console.cloud.google.com/apis/credentials
# Leave blank to disable Google login (email/password will still work).
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# GitHub OAuth — https://github.com/settings/developers
# Leave blank to disable GitHub login (email/password will still work).
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""
